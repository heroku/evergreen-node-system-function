#!/usr/bin/env bash

set -eo pipefail

cat << EOF > "$2"
  [[requires]]
  name = "riff-invoker-node"
  [requires.metadata]
  fn = "../layers/heroku_evergreen-node-system-function/system-function"
EOF

exit 0
